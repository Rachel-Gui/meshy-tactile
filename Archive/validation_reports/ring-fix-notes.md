# ring.gh 修复记录

已在本机 Rhino 8 中重新打开保存文件并求解验证。

- 原文件的编织偏移为 +1/-1，仅有 10 个内部交叉点。原筛选只排除左端圆环，保留右端 10 个端点，因此向要求 30 点的 Python 组件输入了 20 点，导致 HeatMesh 为空。
- 按内嵌脚本要求的 30 个内部交叉点，将编织偏移设为 +2/-2，并增加右端圆环距离筛选。当前两端节点均被排除，留下 30 个不同的内部交点。
- Heat radius 设为 3 mm，Heat mesh size 设为 0.6 mm，预览偏移设为 0.05 mm。仅显示 HeatmapMesh，关闭辅助几何预览并取消选中高亮。
- 保留原有直径、长度、条带宽度及厚度参数。原文件备份为 `ring.before-fix.gh`。

验证结果：20 个网格片段、13,068 个顶点、11,456 个面；网格有效，顶点颜色完整，所有组件运行错误为 0。重新打开保存文件、30 点唯一性和两端排除、输入 0/1 的颜色响应均通过。视口图见 `ring-heatmap-preview.png`，记录见 `ring-validation.txt`。

## 使用

在 Rhino 8 的 Grasshopper 中打开 `ring.gh`，启用求解和 Shaded 预览。`HeatmapMesh` 是最终彩色模型，`Status` 面板应显示 OK。修改连接到 Repeat Data 的 0.3 滑块可测试颜色响应。

当前 Values 仍是原文件的 30 个重复测试值 0.3，未连接实时串口。接入实测数据时，替换 Values 输入为 30 个 0–1 数值，顺序应与 SensorIntersections 列表逐一对应；实际硬件通道顺序尚未标定。30 点布局是依据 GH 内嵌脚本的明确要求；如果实物采用 +1/-1 编织，则应使用 10 个内部传感器布局，不能沿用本文件的 30 点对应关系。
